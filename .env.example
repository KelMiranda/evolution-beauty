DATABASE_URL=postgres://local_user:replace_with_a_strong_password@localhost:5432/acoes_local
INITIAL_ADMIN_EMAIL=admin@example.com
INITIAL_ADMIN_PASSWORD=replace_with_a_long_random_password
NODE_ENV=development
PORT=4321
HOST=0.0.0.0

# The public origin served by Astro. Used by the
# POST /api/courses/<id>/public-link endpoint to generate HashRouter URLs.
PUBLIC_SITE_URL=http://localhost:4321

# Cloudflare Turnstile (anti-bot)
# Get keys from https://dash.cloudflare.com/ under Turnstile.
# Leave empty in development to skip the widget and server verification.
TURNSTILE_SITEKEY=replace_with_public_sitekey
VITE_TURNSTILE_SITEKEY=
TURNSTILE_SECRET=replace_with_a_long_random_secret
